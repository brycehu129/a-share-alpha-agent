# A股 Alpha Agent

当前阶段：通过 GitHub Actions 获取腾讯行情，生成6指数市场快照，并将报告与原始字段追加保存到私有仓库的 `market-data` 分支。

[打开报告历史](https://github.com/brycehu129/a-share-alpha-agent/tree/market-data) · [运行市场快照工作流](https://github.com/brycehu129/a-share-alpha-agent/actions/workflows/market-report.yml)

## 手动更新

打开工作流 → Run workflow → 选择 master → Run workflow。完成后，报告历史入口会增加一份报告。工作流还会在另一台全新运行机器恢复已提交的数据，验证报告内容与校验和。

目前没有开启定时运行。无需服务器或 API Key，使用仓库内置 GITHUB_TOKEN 写入数据分支。私有仓库 Actions 运行受账户额度约束。

## 数据与程序

- `master`：Python 标准库采集器、报告程序、工作流和测试。
- `market-data/records/`：按运行编号保存的 JSON，包含原始字段、来源、时间和 SHA-256。
- `market-data/reports/`：可直接阅读的 Markdown 报告。
- 临时 SQLite 和诊断日志作为 Actions 附件保留7天；正式历史保存在数据分支，不依赖附件保留期。
- 历史禁止程序覆盖，但仓库管理员可修改 Git 历史，因此不等同防篡改审计。

采集失败也会保存失败报告，并使最终验证失败；不冒用旧行情。报价陈旧或日期不齐会明确标注。重复采集不计为独立交易样本。

## 尚未实现

交易日历、全市场个股扫描、历史日线、板块评分、新闻、LLM 分析、预测胜率、虚拟交易与 Dashboard 数据接入均未完成。现有 Dashboard 仍为演示数据；本程序不生成买卖建议。

项目目标参数：10万元虚拟本金、无杠杆、3–20交易日、最多3只持仓，最大回撤约束50%。这些是后续策略约束，当前并未执行交易。

## 本地验证

```bash
python3 -m unittest discover -s server -p 'test_report_pipeline.py' -v
```

原始连通性测试说明见 [server/GITHUB_TEST.md](server/GITHUB_TEST.md)。

## 数据访问扩展 0.3

每次报告增加固定5只测试股票的报价，以及3个指数和5只股票的最近最多320条日线。个股未复权与前复权分别保存，不属于推荐名单或全市场扫描。JSON 的 datasets 字段保存各请求来源、时间、响应摘要和日线。

历史日历取3个指数已有日线日期的交集，仅表示观察到的交易日期；未取得完整官方未来休市日历，未来日期与缺失日期均按未知处理，暂不开启自动调度。日线缺失或异常会记录失败并让验证工作流报错，不静默切换复权口径。

这是上文“尚未实现”部分的数据访问进展；历史日线已接入小样本，严格时点回测、全市场覆盖和官方交易日历仍待完成。

## 沪深批量行情 0.4

每份报告新增 `universe`：新浪 hs_a 分页清单、源总数前后核对、去重检查，以及腾讯每批50只股票的报价。仅纳入沪深A股代码，北交所及范围外代码单独列出。该覆盖率以源清单为分母，未与交易所完整名单交叉核验。

失败报价逐股记录，不静默删除。清单不完整或报价覆盖低于90%时验证任务失败；90%只是数据访问报警门槛，不是交易许可。清单完整且少量报价失败时可保存并通过恢复验证，报告仍标记 partial。请求总预算300秒，超时保留部分结果。未开启定时任务。

当前新增的是报价覆盖；全市场日线、历史成分、行业映射、交易资格过滤与主线评分仍未实现。

## 行业与样本强度 0.5

[研究报告历史](https://github.com/brycehu129/a-share-alpha-agent/tree/market-data/research) · [手动运行研究工作流](https://github.com/brycehu129/a-share-alpha-agent/actions/workflows/research-report.yml)

研究工作流读取最近已校验的 universe 报告，不重新抓全市场报价，报告明确标出来源日期。新浪行业成员按分页数量去重校验；映射冲突和未映射代码不参加行业统计。行业按同日有效股票涨跌幅中位数排序，至少5只且组内报价覆盖90%，不是主线模型。

代码哈希排序固定取120只日线样本，每只最多请求100条前复权日线，另取沪深300。20日窗口21个日期与基准完全对齐才计算收益、超额百分点和MA20偏离；输出样本内排名。当前不是全市场日线覆盖，不输出胜率或交易建议。基准价格指数和前复权收益口径差异、幸存者偏差及未过滤交易资格均在报告标注。

原始解析数据与请求摘要保存在 research/运行编号.json，Markdown报告可直接阅读。校验至少108只样本可计算并有行业统计；这是工程数据门槛，不是策略有效性标准。最多3个并行HTTP请求，6分钟请求预算；尚未启用定时。

## 基础过滤与行业源验证 0.6

样本扩大至300只（按基础排除后的代码哈希排序），至少270只通过日期对齐才通过日线工程门槛。按源名称排除 ST/*ST/退市标记，排除缺失、日期不齐及无效开盘/价格。通过这些规则仅表示可参与研究，不表示已核验可交易；官方停牌、上市状态和涨跌停尚未核验。

尝试东方财富批量 f100 行业字段，核对分页总数、去重、有效字段并要求覆盖当前清单至少90%才切换；失败则记录错误并保留新浪分类，禁止拼接不同分类体系。报告显示实际采用来源与验证结果。行业统计也应用基础排除规则。

新增行业备选源：东方财富不可用或覆盖不足时，尝试 BaoStock query_stock_industry；客户端仅在采集任务安装，查询以65秒独立进程超时限制。记录客户端版本、分类体系和源更新日期；混合分类、重复代码、未来日期或超过370天的分类不采用，覆盖低于90%仍回退新浪。不同来源不混用；实际是否采用及失败原因以报告为准。

## Tushare四接口同步

[数据与更新进度](https://github.com/brycehu129/a-share-alpha-agent/tree/market-data/tushare_data) · [运行同步](https://github.com/brycehu129/a-share-alpha-agent/actions/workflows/tushare-sync.yml)

仅使用已实测开放的 stock_basic、trade_cal、daily、adj_factor。仓库密钥名 TUSHARE_TOKEN。手动运行工作流；无定时。每轮最多10日期，先最新缺失日，滚动目标60个已过去的沪深交易日；最近2日重复获取用于源修订。每日期请求全市场数据，单次上限6000条，触及上限视为完整性未知。日线、复权因子原始结果分别保存，匹配并校验后才形成完整日期检查点；失败停止后续日期。

股票主表暂取在市L状态，D/P档案暂缓（当前状态非历史每日状态）；日历按交易所分别保存并复用，目标上一年及当年SSE/SZSE全自然日并核对开市日期。行业来自stock_basic字段，不当作申万分类。数据放在tushare_data，独立于旧研究报告，尚未切换旧排名或接入Dashboard。

每日文件、股票主表和日历可被源修订更新，Git保留旧版本；每次运行报告独立追加，并用独立运行机器按本次数据提交校验文件摘要。初次只完成一批，不代表60日已全部齐备。Token仅在请求步骤注入，代码不输出凭证或请求正文。

实测 stock_basic、trade_cal 限频为1次/小时。同步会根据已归档的限频响应跳过冷却期请求，股票主表失败不阻断日历更新。日历未完成双市场核验时不启动批量日线。当前尚未成功完成首次批量导入，具体进度以数据分支最新报告为准；没有自动重试定时器，冷却后需手动运行。

## Tushare离线分析

[研究报告](https://github.com/brycehu129/a-share-alpha-agent/tree/market-data/tushare_analysis) · [手动生成](https://github.com/brycehu129/a-share-alpha-agent/actions/workflows/tushare-analysis.yml)

只读取已归档数据，不请求行情接口、不使用Tushare密钥。连续21个交易日检查点齐备后计算20日调整收益和MA20偏离；复用已保存且日期完全对齐的沪深300计算超额百分点，缺少基准时留空。未齐备时生成waiting_data状态报告，不输出排名。历史涨幅不代表预测胜率，当前名单有幸存者偏差，不作为回测或交易建议。

工作流绿色仅表示报告成功保存且可在另一台机器重算，不代表行情齐备；请查看报告的ready/partial/waiting_data状态。本任务手动运行，未自动接续采集或发布Dashboard。

## 定时数据日报（当前运行方式）

已启用 `.github/workflows/daily-agent.yml`：周一至周五北京时间09:00和15:30启动（UTC 01:00和07:30），也支持手动运行。GitHub Actions可能排队或延迟，不保证准点完成。此节替代前文“尚无定时”的旧阶段说明。

依次执行Tushare补数、交易日核验、市场采集、行业样本研究、历史窗口分析、日报整理和网页快照导出。已核验休市时跳过行情和行业扫描；日历缺失时标注unknown，只做数据整理，不产生交易信号。历史补数每轮最多10日期，按源限频暂停，后续运行继续。

日报存放 `market-data/briefs/`，网页“报告与来源”新增日报入口。每项列出源时间和是否本次生成；部分采集失败也保留报告，工作流绿色只代表整理与保存完成，不代表全部数据齐备。当前尚无新闻公告理解、盘前胜率预测、交易执行或虚拟账户盈亏。已有真实数据网页在打开或点击检查更新时读取最新导出，并非网页持续轮询。
# 2026-09-16 更新：候选、概率研究与虚拟账户

已启用完整股票清单扫描、固定规则候选筛选、带样本门槛的10日研究概率、不可覆盖的预测日志、5/10/20日验收和10万元虚拟账本。当前详细行为以 [STRATEGY.md](server/STRATEGY.md) 为准，下文早期阶段“尚未启用”的描述是开发历史。

09:00/15:30日报自动调用新流程；`Alpha候选与虚拟账户`工作流也可手动运行。部署无需新增密钥。腾讯日线失败时停止本批请求，并限时尝试独立BaoStock源。候选与收益是否可用由实际数据覆盖和样本量决定，任务绿色不代表全市场数据完整。

新档案：`alpha_data/`（逐股数据及扫描日志）、`predictions/`（冻结预测）、`outcomes/`（到期标签）、`execution_inputs/`（未复权模拟成交输入）、`agent/`（不可覆盖的研究与账户快照）。网页直接读取已导出的这些结果，不在浏览器计算胜率，不发送交易指令。

历史概率有幸存者及当前行业回看偏差，未达30个标签及15个日期组时不提供百分比。虚拟成交与预测标签分开统计，未到期结果不会通过历史回填伪造。全市场覆盖不足95%、缺价和复权异常等均明确展示。当前仍没有自动LLM新闻解读或已经证明的超额收益。


# 2026-09-19 更新：实时行情与 AI 盘后分析

本节推翻上文两处旧说明：README 开头「尚未实现」一节里的「LLM 分析」、以及上一节末尾的「当前仍没有自动LLM新闻解读」。现在有 LLM 分析了（新闻解读仍然没有，见下）。同时上文「本程序不生成买卖建议」也不再准确——AI 层会给出倾向性研判，但它是研究参考，不是已验证的交易信号，也不会自动下单。

新增一条盘后链路：工作日 16:30（排在 15:35 日线流程之后，确保用上当天的候选池）抓实时行情，用最新价重算策略口径的各项门槛，再交给 Claude 做定性研判，产出大盘、持仓、候选池三部分的盘后报告，推送到企业微信，后台「看板 → 盘后分析」（旧地址 `/postclose` 会自动跳转）可查看和手动重跑。

- `live_quote.py`：腾讯批量行情，免token，一次 50 只，把 88 个字段解析成命名字段（量比/换手/振幅/涨跌停价/成交额）。同一接口也取港股和美股指数，但三个市场字段布局和时间格式都不同，按市场分支解析；美股时间戳时区未核验，不据此计算延迟。
- `live_check.py`：**纯规则、无 AI**。用实时价重算 MA5/MA20 偏离、20日高点距离、两条 track 的门槛、冻结计划的 ±3% 入场带、距涨跌停、持仓浮动盈亏。实时昨收与缓存上一交易日收盘对不上时（除权或缓存过期）明确标记，不按错误基准硬算。
- `portfolio_book.py`：真实持仓与自选股手工录入，后台 `/book` 管理。
- `ai_analyst.py` / `claude_client.py`：Claude 研判层，边界见 [AI_LAYER.md](server/AI_LAYER.md)。

**盘中 K 线仍然不参与策略决策。** 上面这些重算只进分析报告，`predictions/`、`outcomes/` 和虚拟账户依旧只认已完成的日线，规则策略的胜率统计不受 AI 层影响。

**AI 的结论未经任何前瞻验证。** 每条判断都留档（模型、prompt 版本、输入输出 token、与送入标的的核对结果），就是为了日后能算出它的真实命中率——在那之前不得把它说成胜率或策略 Alpha。本期没有接新闻和公告，提示词里明确禁止模型提及任何消息面，并有测试守着。

**盘后报告不进公开仓库。** 报告含真实持仓的成本价和数量，只写到服务器本地 `server/data/private/`，通过 Basic Auth 后台和企业微信查看。这也是本仓库第一次引入第三方依赖（`anthropic`），它是可选的：装不上就降级成纯规则报告，既有链路不受影响。
